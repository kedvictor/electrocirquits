<br /><br />

<table class="table">
  <tr>
    <td width="50%">Напрямків</td>
    <td><%= Speciality.count %></td>
  </tr>
  <tr>
    <td>Груп</td>
    <td><%= Group.count %></td>
  </tr>
  <tr>
    <td>Студентів</td>
    <td><%= Student.count %></td>
  </tr>
  <tr>
    <td>Типів оцінювання</td>
    <td><%= Examination.count %></td>
  </tr>
  <tr>
    <td>Оцінок</td>
    <td><%= Ball.count %></td>
  </tr>
</table>

<div id="activity_chart">
  <%= column_chart ActiveRecord::SessionStore::Session.where(['created_at > ?', Time.now - 2.month]).group_by_day(:created_at, format: "%d.%m.%Y").count %>
</div>

